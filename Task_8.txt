2. Create a categories table using Laravel Eloquent. Also in a .txt file mention how to create it using MySQL query. Have the necessary columns along with audit columns as well.

login phpMyAdmin

Click on New - Databases- Enter database name and click on create

CREATE TABLE categories (
id int,
name varchar(20),
is_active boolean,
created_by int(10) DEFAULT NULL,
updated_by int(10) DEFAULT NULL,
deleted_by int(10) DEFAULT NULL
);


4. Using MySQL query, insert a row and provide the query in a .txt file.

INSERT INTO categories (id, name, is_active) VALUES (1, "FRUITS", 1)





